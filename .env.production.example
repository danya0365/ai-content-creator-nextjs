# ============================================
# Production Environment Variables
# ============================================
#
# คำแนะนำ:
# 1. Copy ไฟล์นี้เป็น .env.production
# 2. แก้ไขค่าทั้งหมดให้เหมาะสม
# 3. อย่า commit .env.production ขึ้น git!
#
# ============================================

# ==========================================
# Domain Settings
# ==========================================
# Domain หลักของแอป (ใช้กับ Traefik SSL)
APP_DOMAIN=your-domain.com

# Domain สำหรับ Supabase API
SUPABASE_DOMAIN=api.your-domain.com

# URL ของเว็บไซต์
SITE_URL=https://your-domain.com

# URLs ที่อนุญาตให้ redirect หลัง login
ADDITIONAL_REDIRECT_URLS=https://your-domain.com,https://www.your-domain.com

# ==========================================
# Supabase URLs
# ==========================================
# URL ของ Supabase API (Kong)
API_EXTERNAL_URL=https://api.your-domain.com
NEXT_PUBLIC_SUPABASE_URL=https://api.your-domain.com

# ==========================================
# SSL/TLS Configuration
# ==========================================
# Email สำหรับ Let's Encrypt SSL certificate
# จะได้รับแจ้งเตือนเมื่อ certificate ใกล้หมดอายุ
ACME_EMAIL=your-email@example.com

# ==========================================
# Database Configuration
# ==========================================
# ⚠️ สำคัญมาก! ต้องเปลี่ยน password นี้
# ใช้ password ที่คาดเดายาก อย่างน้อย 32 ตัวอักษร
# สามารถ generate ได้ที่: https://passwordsgenerator.net/
POSTGRES_PASSWORD=your-super-secure-password-change-this-now

# ==========================================
# JWT Configuration
# ==========================================
# ⚠️ สำคัญมาก! ต้องเปลี่ยนค่านี้
# Secret สำหรับ sign JWT tokens
# ต้องยาวอย่างน้อย 32 ตัวอักษร
# สามารถ generate ได้ที่: https://generate-secret.vercel.app/32
JWT_SECRET=your-super-secret-jwt-secret-at-least-32-characters

# ==========================================
# Supabase API Keys
# ==========================================
# ⚠️ สำคัญมาก! ต้อง generate ใหม่
# 
# วิธี generate:
# 1. ไปที่ https://supabase.com/docs/guides/self-hosting/docker#generate-api-keys
# 2. ใส่ JWT_SECRET ที่ตั้งไว้ด้านบน
# 3. Copy ANON_KEY และ SERVICE_ROLE_KEY มาวางที่นี่
#
# หรือใช้ script: node scripts/generate-keys.js

# ANON_KEY - ใช้กับ client-side (browser)
# สิทธิ์จำกัด ตาม RLS policies
ANON_KEY=your-anon-key

# SERVICE_ROLE_KEY - ใช้กับ server-side เท่านั้น!
# มีสิทธิ์เต็ม bypass RLS
# ⚠️ อย่าใช้บน client-side!
SERVICE_ROLE_KEY=your-service-role-key

# ==========================================
# Realtime Configuration
# ==========================================
# Secret สำหรับ Realtime service
# Generate: openssl rand -base64 64 | tr -d '\n'
SECRET_KEY_BASE=your-secret-key-base-at-least-64-characters

# ==========================================
# SMTP Configuration (Optional)
# ==========================================
# ตั้งค่านี้ถ้าต้องการส่ง email จริง
# ถ้าไม่ตั้ง จะใช้ GOTRUE_MAILER_AUTOCONFIRM=true

# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# SMTP_ADMIN_EMAIL=noreply@your-domain.com

# ==========================================
# Application Settings
# ==========================================
NEXT_PUBLIC_APP_NAME="AI Content Creator"

# ==========================================
# Note: Docker Build Args
# ==========================================
# Next.js public env vars ต้องเป็น build-time
# ดังนั้นต้อง pass เป็น build args ใน docker-compose
# หรือ set ใน Dockerfile
